import Vue from 'vue/dist/vue.common.js';
import VueRouter from 'vue-router';
import markdownData from './temp/markdown-data';

console.log(markdownData);
Vue.use(VueRouter);

new Vue({
    el: '#app',
    template: '<div>ehhehe</div>'
});